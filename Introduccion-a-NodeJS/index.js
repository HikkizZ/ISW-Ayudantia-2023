// Función simple que recibe un nombre y lo imprime en consola
function Saluda(nombre) {
    console.log(`Hola ${nombre}`);
}

// Llamamos a la función Saluda con el argumento 'Pipe!'
Saluda('Pipe!');